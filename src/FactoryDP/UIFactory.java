package FactoryDP;

public interface UIFactory {
    //This is factory interface (contains all the factory method) for flutter class

    public Button createButton();
    public Menu createMenu();
}
